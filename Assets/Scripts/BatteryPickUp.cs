using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BatteryPickUp : MonoBehaviour
{
    /*public float BatteryPowerAmount = 50f;
    private Flash FlashLight;

    private void Awake()
    {
        FlashLight = GameObject.FindObjectOfType<Flash>();
    }
    /*void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.CompareTag("battery"))
        {

            //Flash temp = GameObject.FindGameObjectWithTag("battery").GetComponent<Flash>();
            //temp.AddBattery(BatteryPowerAmount);

            //FlashLight.AddBattery(BatteryPowerAmount);
           // other.gameObject.SetActive(false);

        }
    }*/
}
